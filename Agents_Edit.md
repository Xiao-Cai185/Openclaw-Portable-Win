# Openclaw 项目开发记录

## 第一章：项目架构与技术栈
- **项目架构及内容**：基于 Inno Setup 构建的打包与环境配置工程。
- **开发语言/工具**：Inno Setup (Pascal Scripting), Bat (批处理), OS 原生指令。
- **主要功能**：支持将所需文件打包进安装程序中，解压到用户选择的路径，并实现相关的辅助功能，例如向系统写入注册表操作从而添加至环境变量中，以及创建系统桌面快捷方式（“叫醒小龙虾”、“初始化小龙虾”）。

## 第二章：功能开发与修复详细记录

### 2026-03-19 Bug修复：优化解压逻辑，恢复原生效果
- **问题描述**：原有的 setup 过程中进度条直接拉满并在后台执行解压，且解压出来的目录多嵌套了一层 `src` 目录，这是因为调用了 Powershell 的 `Expand-Archive` 解压 `src.zip` 导致的。
- **修复详情**：
  1. 移除了使用 `powershell` 进行外部解压的 `[Run]` 执行逻辑。
  2. 修改 `[Files]` 配置项，废弃预打包的 `src.zip`，改为使用 `Source: "src\*"; DestDir: "{app}"; Flags: ignoreversion recursesubdirs createallsubdirs` 自动将其打包。
- **预期效果**：打包后的安装包将能够通过 Inno Setup 原生的安装进度条显示具体的提取进度，并正确将内部文件提取至目标目录而不产生多余嵌套的 `src`。

### 2026-03-19 功能更新：安装结束时自动跳转官网
- **需求描述**：安装过程结束后，静默、强制地自动打开指定的 URL（`https://hcaicai.top`）。
- **开发详情**：在 `setup.iss` 中引入了 `[Run]` 块，加入指令 `Filename: "https://hcaicai.top"; Flags: shellexec runasoriginaluser nowait`。移除了 `postinstall` 标志，这样就不会给用户提供是否勾选的选择，安装结束前即会直接静默调起默认浏览器打开相应链接。

### 2026-03-19 功能更新：安装向导中文化
- **需求描述**：将原本纯英文的安装界面、按钮、协议和提示文案中文化。
- **开发详情**：在 `setup.iss` 中引入了 `[Languages]` 节，增加了 `Name: "chinesesimplified"; MessagesFile: "compiler:Languages\ChineseSimplified.isl"`，调用 Inno Setup 内置的简体中文语言包，使得安装包在运行时默认以中文展示。

### 2026-03-19 功能更新：定制安装程序与快捷方式图标
- **需求描述**：为主安装包配置专属图标（`install.ico`），并为生成的桌面快捷方式“叫醒小龙虾”配置图标（`start.ico`），“初始化小龙虾”配置图标（`init.ico`）。
- **开发详情**：
  1. 在 `[Setup]` 段添加 `SetupIconFile=install.ico` 改变打包后的主执行程序图标。
  2. 在 `[Files]` 段添加两个 `.ico` 文件的打包配置，以便在其安装到目标目录后供快捷方式读取使用。
  3. 在 `[Icons]` 段的两个对应条目中增加 `IconFilename: "{app}\start.ico"` 和 `IconFilename: "{app}\init.ico"`。

### 2026-03-19 Bug修复：解决由于编译器组件不全导致的中文支持报错
- **问题描述**：原中文化时采用 `compiler:Languages\ChineseSimplified.isl`，但用户的 Inno Setup 编译器根目录下缺少此官方繁/简体中文文件，导致编译报错。
- **修复详情**：
  1. 通过命令行从官方库仓库中（`https://raw.githubusercontent.com/jrsoftware/issrc/main/Files/Languages/Unofficial/ChineseSimplified.isl`）下载了 `ChineseSimplified.isl` 文件到项目的同级目录。
  2. 修改 `setup.iss`，将被调用的语言文件相对路径修改为直接指向本项目根目录的 `"ChineseSimplified.isl"`。

### 2026-03-19 功能更新：卸载时提供清理数据残留选项
- **需求描述**：在卸载环节中弹窗询问用户是否一并删除 `%USERPROFILE%\.openclaw` 文件夹，如果确认，则彻底清空小龙虾的记忆和配置，否则予以保留。
- **开发详情**：
  在 `setup.iss` 原有的 `CurUninstallStepChanged` 卸载事件函数中扩展了对 `usPostUninstall` 阶段的处理：通过内置 API 获取系统目录后拼接得到 `.openclaw` 路径，当检测到其存在时，将使用 `MsgBox` 询问用户意向；若用户点击确认选择清除，则利用 `DelTree` api 强行删除该资料夹下的所有文件和文件夹。

### 2026-03-19 功能更新：补充完善安装程序自身的文件元数据
- **需求描述**：对于最终生成的 `Openclaw_Setup.exe`，让它在右键属性-详细信息中显示文件版本号为 `2026.3.13.05`，版权信息为 `©虎符网络`，文件描述为 `Openclaw离线安装包`。
- **开发详情**：
  在 `setup.iss` 的 `[Setup]` 段中直接附加了一系列 `VersionInfo*` 声明，其中 `VersionInfoVersion=2026.3.13.05`，`VersionInfoCopyright=©虎符网络` 且 `VersionInfoDescription=Openclaw离线安装包`。编译器将把这些属性对应打包成安装包自身的元数据信息。

### 2026-03-19 Bug修复：修复由于卸载时由于常量作用域引起的内部错误导致未弹窗
- **问题描述**：添加清理残留选项后，卸载到最后如果没遇到任何询问直接报“内部错误”（Internal Error），导致卸载弹窗流程中断。原因是原生的 `ExpandConstant('{userprofile}')` 不允许在卸载动作上下文中被解析，容易带来越权访问错误。
  修改了 `CurUninstallStepChanged` 中的地址拼接逻辑，将其改为了安全不受限制的方式：直接通过 `GetEnv('USERPROFILE') + '\.openclaw'` 获取该用户环境变量下的 `.openclaw` 位置，替代了之前会报错的展开常量的写法，解决报错并恢复询问弹窗。

### 2026-03-19 功能更新：优化卸载配置清理时的交互体验
- **需求描述**：原本卸载时使用了简单的 `MsgBox` 询问是否删除配置，希望能像安装向导那样拥有带有确认界面和多选按钮的下一步复选操作步骤体验。
- **开发详情**：
  引入了 Inno Setup 的 `InitializeUninstall` 钩子并在其中使用了 `CreateCustomForm` 动态生成了一个专属卸载环境。包含复选框 `TNewCheckBox` 功能与明确操作说明 `TNewStaticText` 的窗口。在点击"继续"进入正式卸载工作流之后，后备的事件处理器 `CurUninstallStepChanged` 会根据复选框的选择最终决定是否调用 `DelTree` 进行彻底清理。

### 2026-03-19 Bug修复：修正创建自定义窗体时的参数错误
- **问题描述**：在编译脚本时抛出关于无效参数数量的错误（`Invalid number of parameters`），由于 Inno Setup 对未显式传参调用的窗体实例构建方式检测严格导致的语法错误。
- **修复详情**：将代码中的 `Form := CreateCustomForm;` 更改为标准 Pascal 面向对象实例化方式 `Form := TSetupForm.Create(nil);`，从而通过带正确的 `nil` 空值所有者实例满足函数的声明，成功解决了编译故障。

### 2026-03-19 Bug修复：修正反安装时的资源类未找到报错
- **问题描述**：在进行卸载操作运行时抛出 `Resource TSetupForm not found.` 的致命异常，导致卸载流程强制跳出。起因是在 Inno Setup 编译规则中，为了缩减包体积，默认的 `TSetupForm` 界面资源仅在安装向导（Installer）内链入，卸载模块中这部分自带向导样式被完全剔除了。
- **修复详情**：将卸载步骤的 `InitializeUninstall` 中的 `Form: TSetupForm;` 和实例化 `Form := TSetupForm.Create(nil);` 统统由具备依赖注入色彩的 `TSetupForm` 类退化降级为了最纯粹、不挂载任何内部定制 UI 资源的 Windows 原生基础窗口类 `TForm` ，成功在不调用相关缺失资源的情况下从底层构建了提示窗口。

### 2026-03-19 功能更新：优化卸载确认向导的界面排版
- **需求描述**：由于基于底层 `TForm` 原生生成的窗口没有默认指定 UI 引擎相关属性集，在不同缩放（DPI）设定和系统字体作用域下，文字与控件会显得异常干瘪紧凑、重叠且难以看清，需要把确认窗格引入 DPI 适配与标准字集。
- **开发详情**：
  在 `setup.iss` 中修改了 `TForm` 界面的底层实例化规则。为其强制指定了 `bsDialog` 的弹出框风格，并且对全局默认的字体更换为了通用、大气的 `Microsoft YaHei`（微软雅黑）辅以 `9` 号字径。
  最关键的是，现在所有控件的高、宽、外边距间距等各种数值，均全量套用了 Inno Setup 官方提供的动态比例适应函数 `ScaleX(...)` 与 `ScaleY(...)`。这种重构不仅将主窗体缩放至了肉眼舒适的 `460`×`220` 基准分辨率观感边界，同时也保证了高分辨率分屏用户显示时的绝对清晰度与相对一致性坐标展现。

### 2026-03-19 Bug修复：修正强制DPI排版后复选框不可见的问题
- **问题描述**：在排版优化应用了 `ScaleY()` 后，编译发现卸载界面的复选框凭空消失了。
- **修复详情**：出现这一遮挡/消隐错误的核心原因是单纯使用底层 `TForm` 创建组件 `TNewCheckBox` 时它失去了原本默认自带的高度分配或文字内容自撑开的（AutoSize）能力，导致高度在经缩放重新渲染时被压缩到了极限（高度为 0），因此我们在 `setup.iss` 对它的初始化阶段中强制性为其增加了一个容纳排版的绝对高度 `Chk.Height := ScaleY(24);`，让复选框重现。

### 2026-03-20 功能更新：定制 Windows 卸载功能展示界面的专属图标
- **需求描述**：此前打包后的作品在 Windows 控制面板的“程序和功能”（添加或删除程序）中，所展示的图标处于未知或白板状态。由于没特别显性指定给专门的卸载操作界面匹配图标，系统无法追踪。
- **开发详情**：
  在 `setup.iss` 的 `[Files]` 配置段中单独增加了对 `uninstall.ico` 的文件提取打包支持；并在最开始的 `[Setup]` 段中强行指定 `UninstallDisplayIcon={app}\uninstall.ico`。自此，不仅 Windows 能够准确抓取控制面板界面中显示的 Openclaw 卸载图样，整个程序的反安装体系也在外观上达成了一直定制化。

### 2026-03-24 功能更新：将运行入口脚本 openclaw.bat 升级为现代版 openclaw.cmd
- **需求描述**：原本根目录的 `openclaw.bat` 承担了引用环境和分发命令的指责，但它的内容使用的是早期的 DOS Bat 形式（无本地环境变量隔离、无命令透传调用），这在使用 `install.bat` 写入系统环境变量后，作为独立唤起的全局指令在当前终端中执行时，容易引发“污染会话原 PATH 环境变量”等不可预料的问题。
- **开发详情**：
  1. 将文件扩展名及内容从 `openclaw.bat` 重构为现代标准的 `openclaw.cmd`。
  2. 脚本顶部引内装入了全局状态隔离声明 `setlocal`，保障仅该次命令启动时合并调用了 Node，执行结束之后 `PATH` 会立刻复原，保护环境纯净不受到污染。
  3. 修改为了规范安全的带引号的绝对路径定义：`set "NODE_HOME=%SCRIPT_DIR%node"`，有效规避因为外部各种安装路径空格等带来的意外报错风险。
  4. 加入了最终调用的回溯退出行为：采用 `call` 的形式引导核心脚手架动作，末尾抛出系统标准中断响应规范 `exit /b %ERRORLEVEL%`，这将完美保证了全局环境后续捕捉任意构建失败的子命令报错与拦截响应！

### 2026-03-31 Bug修复：修复快捷方式所指向的批处理文件输出乱码
- **问题描述**：`src/start.bat` 与 `src/init.bat` 在控制台中回显（echo）中文字符时由于编码紊乱展现出“TipsСϺ˯ѻῴ”样式的生僻文本乱码字符。
- **修复详情**：
  主动利用 Python 环境按原操作句法结构把纯粹清晰的中文提示语重新写回到这两个 Bat 中。使用了原生的 ANSI (GBK) 编码以及 `CRLF` 的 Windows 换行符系统进行全量复写，确保后续 Inno Setup 自动构建桌面快捷方式去启动它们时能够完美被 Windows 默认中文字符集所解析，不再输出损坏乱码。

### 2026-04-22 功能更新：拆解启动与网关脚本入口，重构图标存储体系
- **需求描述**：将曾经的唤起入口分离拆解：原“叫醒小龙虾”对应的快捷方式变更绑定至 `gateway.bat`；补入独立的“启动小龙虾” `start.bat` 新入口。与此同时，将零散平铺在环境内的各类 `.ico` 图标统筹归纳进专属的 `icon` 子目录中以保持项目结构清爽。
- **开发详情**：
  1. 主动创建了 `icon` 归档目录，将包括安装、卸载、启动、初始化等所有的图标全部规整转移进去。并修复并规范化了你刚刚引入的 `start.bat` 与 `gateway.bat` 的 ANSI 编码与防乱码中文文案。
  2. 彻底重写构建脚本 `setup.iss` 中的映射关系：
     - 修改顶部 `[Setup]` 内置的图标和卸载图标挂载来源，使其精确指向打包前的 `icon\` 目录和打包后的 `{app}\icon\` 目录。
     - 配置 `[Files]` 段令所有打包的图片资源在安装时均统一解压释放在 `{app}\icon` 子层内。
     - 在 `[Icons]` 生成快捷方式部分：更新了“叫醒小龙虾”使之调用新的 `gateway.bat` 及专属 `gateway.ico` 图标；平铺扩充了一条全新任务块，用于在桌面上同步创建“启动小龙虾”并完美调用 `start.bat` 和 `start.ico`。

### 2026-04-22 功能更新：新增 NPM 内置升级脚本 `update.bat`
- **需求描述**：为了方便后续随时更新安装目录下的 Openclaw 包文件，不依赖用户的全局 Node 环境，且不将其写入目前打包的安装包流中，需新增一份通过内部自带 NPM 的指定路径定向升级脚本。
- **开发详情**：
  在项目根目录下新编写了 `update.bat` 脚本：
  1. 通过组合 `%~dp0` 拼凑出指向内部预先释放好的 `\src\node\npm.cmd` 作为独立包管理器执行路径，确保隔离用户本机系统环境干扰。
  2. 加入了 `%~1` 参数解析逻辑：支持在终端里以类似 `update.bat 2026.3.24` 的形式强行指定想要回滚或跨版本的标签更新；如果用户直接双击运行，则会自动 fallback 兜底默认拉取 `latest` 最新版。
  3. 通过命令 `npm install openclaw@<version> --prefix "%OPC_DIR%"` 实现静默覆盖目标安装目录 `\src\opc` 内的模块内容，为了防止国内网络超时，主动配置了 `--registry=https://registry.npmmirror.com` 淘宝镜像源提速。为了防乱码问题依然使用了全英文进行编写。

### 2026-04-28 功能更新：重构 `update.bat` 引入动态版本抓取与交互式升级
- **需求描述**：将曾经的更新脚本转移至 `src` 内部层级，提升脚本智能度：自动拉取最新的线上发布版本号并将其组装为列表菜单，供用户通过输入数字 `1-n` 快捷挑选指定版本进行覆盖安装更新。
- **开发详情**：
  1. 将根目录残留的旧 `update.bat` 清除，并在 `src\` 目录下全新创建了交互版 `update.bat` 且锁定了 ANSI 中文防乱码。
  2. 在更新流程最开始强行切入 `call "openclaw.cmd" gateway stop`，保障升级前后台相关旧网关服务完全终止。
  3. 通过内嵌 PowerShell 向 Github API 请求发布列表；对抓取到的标签（如 `v2026.3.24`）应用了字符切割逻辑，动态剔除了起始的字母 `v` 从而还原出纯净版本号列表数组。
  4. 设计了交互表单：陈列出全部可用版本，用户键入 `1-n` 即可映射选中对应版本；另外预留了 `0` 号选项作为兜底，当选用它时将直接填充指令拉取 `latest` 版。将所选内容无缝拼接向尾部执行 `npm install openclaw@版本号 --prefix...` 的动作。

### 2026-04-28 功能更新：`update.bat` 突破拉取数量并新增自定义输入模式
- **需求描述**：由于 GitHub API 默认的页面显示限制，旧逻辑最多只能拉取展示最新的 30 条版本。不仅需要打破该数量上限展示全部历史，还需要在菜单内额外增加一个能够由使用者自己强行输入特殊版本号的功能。
- **开发详情**：
  1. 将原本的单次提取请求扩写成了携带分页请求参数的连续轮询：指令变为内置了 `per_page=100` 以及动态递增的 `page` 指针通过 `do...while` 的循环结构拉取，直到检测不到后续版本数据，成功突破接口数量界限拉取全部归档包。
  2. 为交互表单首部特地新增了选项：`[-1] 手动输入自定义版本号`。当选择 `-1` 号时，系统将暂停列表分配动作并打开独立二次输入模式，允许用户直接凭空填入任意具体版本（以便测试某些尚未展示但存在的特殊分支包），最后平滑无缝地喂给 NPM 去指向升级。
  3. 脚本继续依靠 Python 层转换以确保严格的中文编码不乱序，使用体验得到大幅度完善。

### 2026-04-28 功能更新：`update.bat` 使用原生 NPM 底层请求全面替代 GitHub 接口
- **需求描述**：此前的更新脚本基于调用 GitHub API 获取包的版本，但受限于页面限制及额外的字符清洗负担。为了简化并且更直接地匹配包管理器本身，要求全面换用 `npm view openclaw versions` 命令来提取和读取发行库底层全部归档号。
- **开发详情**：
  1. 重写了拉取模块的指令，抛弃了复杂的 `Invoke-RestMethod` 针对远端接口的循环探测与处理，采用直接驱动内部自带原生环境 `npm view openclaw versions --json --registry=https://registry.npmmirror.com` 发出访问。
  2. 结合 PowerShell 轻量化的流水线级 `ConvertFrom-Json` 处理机制，自动将从 npm 中请求回来的巨量 JSON 数组展平分解成了最干净的按行输出样式喂给外层的交互式列表。
  3. 因为原生包管理器的版本管理规范不会自带杂余的类似于大写或者小写 `v` 的干扰，所以连带将原有庞大且多余的防污染字符切分清洗代码一并精简删除。保留了全交互输入模式并重新依靠 Python 固定死了 ANSI 无错编码。

### 2026-04-28 Bug修复与功能更新：`setup.iss` 环境配置层级修正与追加桌面入口
- **需求描述**：当安装包拥有自提权需求（`PrivilegesRequired=admin`）被执行时，底层调用的 `HKCU`（HKEY_CURRENT_USER）由于特权降级偏移，会导致原本预期的环境变量写入了 Admin 临时分配层级而非真实用户层，导致用户运行不了程序。并要求为新编写的 `update.bat` 制作专门的触发快捷键。
- **开发详情**：
  1. 修改了 `setup.iss` 底部的 `AddToPath` 和 `RemoveFromPath` 两大注册表底层读写函数。将 `HKCU` 改为写入机器硬层全局级 `HKLM\System\CurrentControlSet\Control\Session Manager\Environment`，顺势利用起已有的 Admin 提权状态向全局播发 PATH，完美跳出了账号权限上下文隔离的 bug 陷阱。
  2. 在包顶端 `[Setup]` 补入了 `ChangesEnvironment=yes` 语句。Inno Setup 会借助此标识自动在释放文件和配置后，向 Windows 全系统广播环境变量刷新的 `WM_SETTINGCHANGE` 消息，再也无需重启电脑即可直接生效。
  3. 修改了 `[Icons]` 桌面创建段块：专门扩写了一条 `Name: "{commondesktop}\切换小龙虾版本"` 的打包参数，并在目标内部挂载了 `update.bat` 实体文件与 `{app}\icon\install.ico` 升级专用图标。

### 2026-04-28 功能更新：`setup.iss` 引入自动版本检测与智能安装防呆闭环
- **需求描述**：为了避免用户在使用脱机包更新时引起版本混乱，安装包在启动时必须拥有“环境自我探查能力”：当电脑已经装有旧版时，主动弹窗询问是否升级覆盖；当用户误用老安装包去覆盖全新环境时，需要立刻引发拦截弹窗并提供降级警告。
- **开发详情**：
  1. 向 `[Code]` 区块顶层注入了 `GetInstalledVersion` 探查函数：在 `InitializeSetup` 安装初始握手阶段，自动探底遍历机器系统注册表中 `Openclaw-Portable_is1` 的卸载面版挂载点，反向提取出现有安装环境残留的 `DisplayVersion` 值。
  2. 为了彻底解决类似 `2026.3.13` 和 `2026.10.1` 因点号分段位数不同引发的字符串对比错乱，编写了底层的 `PadVersionStr` 算法。将其切割补零规整化（例如 `2026.0003.0013`）后再做严谨比对。
  3. 最终实现了弹窗阻断与分流保护：若判定为正常升级动作，则输出覆盖提示引导下一步；若识别出即将发生倒退降级，则切断流水线弹出强烈的 `mbCriticalError` 警告，明确引导用户前往控制面板先卸载新版后再安装，形成了极为严谨且防呆的完整商业发版业务闭环。

### 2026-04-29 功能更新：整理 `setup.iss` 桌面外观并实现快捷方式文件夹归档
- **需求描述**：由于启动、网关、更新等系统入口激增，全部平铺至系统桌面会显得极其杂乱。现需要将这些真实的 `.lnk` 快捷方式暗中存放进 `AppData`，再向桌面仅仅投射出一个统筹的“文件夹快捷方式”，并且向系统的开始菜单追加完整的全套快捷列表（附带上卸载入口）。
- **开发详情**：
  1. 重写了 `[Icons]` 区块，清除了原先直接向 `{commondesktop}` 零散抛洒图件的行为。
  2. **开始菜单合集化**：利用自带的 `{group}` 变量，把五套快捷命令（叫醒、启动、初始化、切换版本、包括新增的**卸载小龙虾**）整整齐齐地收纳进了系统的开始菜单 `Openclaw-Portable` 目录下。
  3. **实体文件后台收容**：在用户的私有缓存结构 `{userappdata}\Openclaw-Portable` 内如法炮制生成了一套真实的源文件 `.lnk` 集合。同时引入了 `[UninstallDelete]` 确保卸载时该收容目录会被一并抹除。
  4. **桌面极简级引流**：在桌面仅生成一枚名为 `Openclaw 工具箱` 的主入口，并穿上你指定的 `all.ico` 外衣。其底层原理是利用 `explorer.exe` 参数化指向了上述的 `AppData` 目录，用户双击打开就像打开一个普通文件夹一样干净利落。
  5. 把 `[Files]` 段的散装复制全部简化成通配符 `*.ico` 防止未来添漏，并将生成桌面的 `[Tasks]` 彻底改为了全天候强行勾选状态。

### 2026-04-30 功能更新：`update.bat` 彻底剥离 PowerShell 依赖，实现纯 CMD 化 JSON 数组解析
- **需求描述**：部分用户的计算机由于严苛的安全策略或组策略限制，禁用了 PowerShell（即 PS1 脚本执行权限报错），导致此前利用 PowerShell 解析 JSON 版本号的升级方案失效。需求彻底弃用 PowerShell，强制要求使用最基础的纯 CMD 批处理代码完成 NPM 源的版本号拉取与数组清洗。
- **开发详情**：
  1. 彻底移除了上一版内置的 `powershell -Command` 与管道 `ConvertFrom-Json` 组件。直接改用原生的 `npm view openclaw versions` 输出带格式的原始数据包，将其投喂给纯净版的 CMD `for /f` 逐行读取流中。
  2. 运用了原生 Batch 的高阶字符串置换（String Substitution）技术：在拿到数据行的那一刻，动态地将文本内的左括号 `[`、右括号 `]`、单引号 `'`、双引号 `"` 甚至逗号 `,` 全盘替换成普通空格符（`set "line=!line:[= !"`）。
  3. 在清洗出纯净环境后，紧接着嵌套一层内部次级循环 `for %%V in (!line!) do`，完美利用了 CMD 遇空格自然断词、切割枚举的古老特性。优雅地把原本一堆大便一样的乱格式，顺畅地拆解为一个个独立、干净的版本号，实现了完全无需借助第三方运行库就能完成极其复杂的 JSON 到终端菜单映射显示。

### 2026-05-01 功能更新：安装包追加 Node 与 Git 环境挂载可选任务
- **需求描述**：为了方便部分重度使用者直接在命令行终端全局唤起内置的环境引擎，要求在安装向导面板增加两项可选配置，支持将自带的 Node.js 及 Git 推入系统的全局 PATH，并且默认保持不勾选以防止污染不需要它的用户电脑。
- **开发详情**：
  1. 修改了 `setup.iss` 的 `[Tasks]` 区域。除了固有的桌面快捷方式和主程序环境勾选项外，横向平级延展出了两个全新任务节点 `addnodetopath` 与 `addgittopath`，均加上了 `Flags: unchecked` 的安全设定，让其默认处于非活动勾选状态。
  2. 扩写了底层的 `CurStepChanged` 动作响应块。在文件全部解压完毕（`ssPostInstall`）的切面中增加动态判定：分别探测 Node 与 Git 的复选框是否被激活。若捕获到选中，则当即调用自带的提权注册表挂载函数，将 `{app}\node` 与 `{app}\git` 绝对路径强制植入宿主机环境变量。
  3. 补全了相对应的无痕清退逻辑：在卸载切面 `CurUninstallStepChanged` 内追加了对应的双环境擦除指令，保证未来无论用户怎么操作，卸载时相关污染项一定被抹平抽离。

### 2026-05-01 功能更新：安装向导引入全智能环境探测与按需解压引擎
- **需求描述**：安装包需要具备环境感知能力，动态判定系统中是否存在 Node.js 与 Git。根据探测结果自动帮用户做出最佳抉择（动态打钩或取消）。并且针对附赠的 Git 环境，要求实现“不勾选就坚决分解压，勾选了才提取”的瘦身机制。
- **开发详情**：
  1. 引入底层终端探针：在 `[Code]` 区域核心打造了 `HasCommand` 探测函数。通过 `Exec` 函数静默调起 `cmd.exe /c` 执行 `node -v` 和 `git --version`，截获操作系统的 `ResultCode` 零退出码来精准探明环境存在状态。
  2. 动态接管任务面板：注入了 `CurPageChanged` 切面监听任务页。翻到该页瞬间，后台光速执行探测并操作可视化控件树 `WizardForm.TasksList`。如果无环境，主动打勾推荐；如果系统已自带 Git，则当场将复选框 `ItemEnabled` 锁死变灰禁用，并在尾部追加提示“(系统已安装，已忽略)”。
  3. 结构重组与条件解压：在 `[Files]` 段运用高级打包技巧，利用 `Excludes: "git,git\*"` 将庞大的 Git 库从全量源 `src\*` 中剥离。随后单独将 `src\git\*` 开辟新流并强制锚定 `Tasks: addgittopath`。一旦向导层 Git 未被勾选，整个底层释放流将直接绕开该文件夹，实现了体积与时间的极致节省。

### 2026-05-03 Bug修复：`update.bat` 强化原生解析的源格式绝对规整化
- **问题描述**：此前剥离 PowerShell 时使用裸的 `npm view` 获取版本，在不同版本的 NPM 或不同网络回显策略下，输出的数组换行格式可能会出现畸变，导致批处理原生切分存在隐患。
- **修复详情**：在请求体中硬性补回了 `--json` 声明（即执行 `npm view openclaw versions --json`）。这能强制迫使远程流始终吐出极度刻板规整的 JSON 数组。利用我们之前手写的原生“括号引号全量大清洗”替换法则，不仅不会被 JSON 阻碍，反而能更加坚不可摧地将其秒切成干净的纯数字版本号选项，彻底堵死了格式错乱的可能。

### 2026-05-03 功能更新：精简 Node 目录映射与 NPM 模块损坏致命拦截
- **需求描述**：用户重新部署了完整的官方 Node 压缩包，为了提升工程整洁度将目录统一规范为 `src\node`。另外，当 NPM 因为长路径文件丢失导致核心模块抛出 `error` 或 `MODULE_NOT_FOUND` 的报错 JSON 时，会被原生循环错误切割展示为版本号，急需补全环境防呆兜底机制。
- **开发详情**：
  1. 目录架构大清洗：全景扫描并批量重写了全栈脚本与打包配置文件。将 `setup.iss`、`src\update.bat`、`src\openclaw.cmd` 等底层基建里所有指向长串名称的节点挂载点，全部精简化替换映射至全新的 `node` 目录。
  2. 极速原生崩溃探针拦截：在 `update.bat` 读取拉取流的深层循环内，没有采用缓慢的外部 `findstr`，而是注入了 Batch 自带的最快文本比对语法（`if not "!line:error=!"=="!line!"`）。能在毫秒级瞬间嗅探到含有 `error` 或 `MODULE_NOT_FOUND` 的崩溃字节流！
  3. 当防护网闭合捕获到报错流时，不仅会当场清空所有的脏变量阻断渲染，更会抛出极其醒目的红色预警警告用户：明确指出这是长路径截断引发的 NPM 模块底层丢失，并附上强制采用 7-Zip/WinRAR 重新解压的正确解决方案。完美补全了脱机包工具链最薄弱的一环。

### 2026-05-03 Bug修复：`setup.iss` 重构卸载逻辑，实现“原汤化原食”无损清理
- **问题描述**：此前的环境清理脚本存在暴力剥离逻辑——无论用户安装时是否勾选了“添加 Git 到环境变量”，卸载器都会在最后一步无脑执行 `RemoveFromPath`。虽然该函数自带绝对路径匹配，但在某些特定情况（如用户自己魔改了环境变量，或者路径出现意料外重叠）下，会导致原主机的相关环境变量被误杀清理，造成严重的环境破坏。
- **修复详情**：
  1. 引入安装态持久化记忆体：在 `CurStepChanged` 的挂载切面（`ssPostInstall`），拦截环境写入动作。只要执行了 PATH 添加，就利用 `SetIniString` 向安装目录后台抛出一个高度隐秘的 `install_vars.ini` 记忆文件，精准打上标记（例如 `GitPathAdded=1`）。
  2. 智能卸载与环境溯源：在 `CurUninstallStepChanged` 彻底废弃了无脑三连删。卸载器现在必须先读取那个 `ini` 账本，当年安装程序**亲手写了哪条变量，现在就只去擦除那条变量**！绝不多碰原系统的一草一木。
  3. 全闭环痕迹销毁：在读取完账本、精准完成变量复原后，调用 `DeleteFile(IniPath)` 将该记账本无痕焚毁，实现了100%安全无损的装卸循环。

### 2026-05-03 Bug修复：`setup.iss` 解决 Inno Setup 错误排除底层 NPM Git 模块的致命重名 Bug
- **问题描述**：在先前实现“按需解压 Git”时，使用了 `Excludes: "git,git\*"` 的打包策略。新机安装后测试发现，内置 Node.js 完整且能输出版本，但只要执行 `npm view` 就会崩溃报 `Cannot find module '@npmcli/git'` 的深层缺失错误。因为报错指向 NPM 内部，排障极其困难。
- **原因剖析**：由于 Inno Setup 对没有加上反斜杠的匹配规则默认开启“全局穿透匹配”。`"git"` 这一规则不仅排除了 `src\git` 这个环境包，还**物理阉割**了隐藏在 `src\node\node_modules\npm\node_modules\@npmcli\git` 中的核心模块文件夹！因为那个模块的底层名字刚好也叫 `git`。
- **修复详情**：在 `[Files]` 的按需解压配置中，将排除规则硬化为 `Excludes: "\git,\git\*"`。利用前置反斜杠强制声明该规则为“根目录锚定绝对路径匹配”。彻底杜绝了将深埋在应用底层的同名核心模块误杀排除的惊天大坑。

### 2026-05-03 功能完善：`setup.iss` 补充卸载时强杀残留计划任务指令
- **需求描述**：在小龙虾服务的日常使用中，可能会由脚本向系统注入名为 `OpenClaw Gateway` 的后台计划任务来维持保活或自启。如果不做清理，在卸载 Openclaw 后，该计划任务将成为死链且不断抛出系统报错，造成系统垃圾残留。
- **开发详情**：
  1. 在 `setup.iss` 的 `CurUninstallStepChanged(usUninstall)` 卸载起始切面中，横向补充了一道底层静默执行命令。
  2. 利用提权的 `Exec` 组件，直接静默调用宿主机原生指令：`schtasks /Delete /TN "OpenClaw Gateway" /F`。
  3. 这样一来，一旦触发卸载，不仅能做到环境变量无损复原、应用级配置文件按需拔除，甚至能深入操作系统级别的任务调度器，连根拔起与之关联的僵尸守护任务，达成了更为干脆彻底的“抹除式卸载”。

### 2026-05-03 Bug修复：`setup.iss` 解决修改 Task Description 后探针判定失效的问题
- **问题描述**：用户手动修改了 `[Tasks]` 区域里 `addgittopath` 的 Description 描述（去掉了“添加 git”等字眼），导致我们在 `CurPageChanged` 切面中原本写死的 `Pos('添加 git', ItemCaption)` 文本匹配探测瞬间抓瞎，进而让整个基于环境变量探测的灰色禁用逻辑全部宕机失效。
- **修复详情**：在 `[Code]` 区，彻底精简了探针的锚定关键词。将 `Pos('添加 git', ...)` 和 `Pos('添加 node', ...)` 等长段落强匹配，全部降维成了极其宽泛且鲁棒的 `Pos('git', ...)` 和 `Pos('node', ...)`。这使得无论未来打包者怎么修饰或者润色安装页面的描述性文案，只要任务依然与 node 或 git 挂钩，我们的自动挂载与锁死引擎就绝对不会脱靶失效。

### 2026-05-03 功能扩展：新增配置与自我诊断入口
- **需求描述**：项目工程中新增了 `configure.bat`（快速配置）与 `doctor.bat`（自我诊断）两个高频管理脚本，需要将它们纳入系统的官方快捷入口矩阵。
- **开发详情**：
  1. 在 `setup.iss` 的 `[Icons]` 节点下，同步追加了两组指令规则。
  2. 使得安装后不仅能在系统的「开始菜单」文件夹看到 "配置小龙虾" 与 "自我诊断"，同时这些快捷方式也会一并释放在用户目录的 `{userappdata}\Openclaw-Portable` 隐秘归档池中，供桌面的“工具箱”入口做统一管理收纳。

### 2026-05-03 功能增强：防止 Openclaw 环境变量与系统原生全局包冲突
- **需求描述**：如果用户已经在物理机上使用 `npm install -g openclaw` 安装过全局包，再在安装程序中勾选“添加 Openclaw 到 PATH”，会导致我们的便携版网关执行脚本与系统的全局包指令发生抢占冲突。
- **开发详情**：
  1. 拓展了智能环境探针功能：在 `setup.iss` 的探测切面中追加了 `HasCommand('openclaw --version')` 动作。
  2. 防火墙级锁死：一旦探针发现宿主机能够回显 Openclaw 版本，安装包将立刻在附加任务页中把“添加 Openclaw 到 PATH”选项变成灰色不可选状态，并标记为“(已安装全局包，已忽略)”。
  3. 保障原生纯净：该设定完美隔离了系统级原生应用与我们这套便携式脚本的变量污染，而我们的所有快捷方式因均已采取局部变量预定义（在 `.cmd` 头处理），所以执行仍畅通无阻不受任何影响。

### 2026-05-03 严重Bug修复：彻底重构底层 PATH 读写逻辑，解决“系统环境变量被意外抹除”的史诗级隐患
- **问题描述**：用户反馈，哪怕卸载时的 `install_vars.ini` 成功阻止了卸载脚本去卸载 Git，他们系统中原本安装的 Git 甚至其他命令行工具，依然会在卸载 Openclaw 后诡异地失效或者报找不到命令的错误，看起来就像是系统 PATH 被暴力清洗了一样。
- **原因剖析**：这其实由 Inno Setup 底层代码里隐藏的两个极其凶险的逻辑地雷同时引爆所致：
  1. **注册表类型强转降级（致命级）**：原先使用 `RegWriteStringValue` 去写回 PATH，这会把 Windows 环境里的动态可扩展字符串（`REG_EXPAND_SZ`）直接强行降级锁死为普通静态字符串（`REG_SZ`）。这导致用户的 PATH 里面原本带有的 `%SystemRoot%`、`%USERPROFILE%` 等所有动态占位符彻底失去解析能力。系统压根找不到这些原始路径，从而导致原生环境大面积瘫痪，用户误以为是“被抹除了”。
  2. **字符串无差别截断（毁损级）**：原先使用 `StringChangeEx` 的无脑替换逻辑，这是一种危险的纯文本替换。比如要移除 `{app}`（例如 `D:\Openclaw`），在扫过 `{app}\node` 时，会直接把前缀消融掉，把它腰斩变成残缺的 `\node` 永远留在系统里。
- **修复详情**：
  1. 彻底废弃文本替换，自行编写了一套纯数组切割库 `Explode`。现在的 `RemoveFromPath` 会将长长的系统 PATH 严格按照分号 `;` 切割成独立的元素组成的数组，然后进行**100%全词精准比对**（CompareText）。只剔除完全一样的那一段，绝不波及任何相似前缀的兄弟节点。
  2. 全面改写 API 调用，将写入指令由 `RegWriteStringValue` 强制更正为 `RegWriteExpandStringValue`。这保证了在剥离属于我们的环境变量后，完美维持系统的 `REG_EXPAND_SZ` 高级注册表类型，原系统内的所有 `%` 通配符变量完好无损，环境彻底恢复纯净的如初状态。

### 2026-05-03 功能扩展：新增“恢复出厂设置”脚本与全量清理逻辑
- **需求描述**：提供给用户一键销毁残留配置文件（恢复出厂）的快捷通道。此外，在卸载 Openclaw 时，由于后续使用期间 npm 会向 `opc` 目录下载新的 node_modules，这些后来加入的文件不在 Inno Setup 原始的安装文件追踪列表中，导致无法被自动删除而残留。
- **开发详情**：
  1. **新建脚本**：在 `src` 下新建了 `recovery.bat`，利用原生 GBK 编码输出中文，带有红色警告意味的免责提示。用户在控制台键入 `Y` 确认后，脚本会静默执行 `rmdir /s /q "%USERPROFILE%\.openclaw"`，一键扬灰所有的历史记忆和 Skill 配置。
  2. **挂载入口**：在 `setup.iss` 中为它专门配上了与卸载、配置等看齐的独立快捷方式（命名为“恢复出厂设置”），并同样收录于系统的「开始菜单」和桌面「Openclaw 工具箱」内。
  3. **终极卸载清理**：在 `setup.iss` 的 `[UninstallDelete]` 区域增加了一句 `Type: filesandordirs; Name: "{app}\opc"`，赋予卸载器直接把整个 `opc` 文件夹执行强制抹除的底线指令，彻底解决后续更新产生的残留垃圾文件问题。

### 2026-05-03 体验优化与Bug修复：升级脚本 `update.bat` 追加版本验证反馈与当前版本提示
- **需求描述**：
  1. 用户在执行版本切换（升级或回退）后，只有一行“更新成功”的提示，由于缺乏直观的当前版本打印，用户很难直观确认实际生效的版本是否如预期一致。
  2. 在面对多达几十上百个 NPM 可用版本的列表选择页面时，用户缺乏“我现在是什么版本”的基准参考系。
  3. （修复）在此前利用 `for /f` 捕获命令输出以赋值变量时，由于 Windows CMD 臭名昭著的内部转义机制及括号嵌套问题，导致原代码抛出 `不是内部或外部命令` 的严重执行流崩坏错误。
- **开发详情**：
  - **更新后即时验证**：修改 `src/update.bat` 脚本的末尾成功回调区域（`if !errorlevel! equ 0`）。在宣布更新成功后，新增了一步 `call openclaw.cmd --version` 动作。这使得脚本在结束暂停（`pause`）前，会强制调用一次客户端打印并验证当前的最新版本号。
  - **选单前置提示（极简直出）**：彻底抛弃了脆弱的 `for /f` 变量捕获，改为直接利用原生的流重定向。使用 `<nul set /p="当前安装版本："` 打印不换行前导文本，紧接着原生执行 `call openclaw.cmd --version 2>nul`，从而把版本号极其稳健且优雅地直出在终端上，彻底根绝了解析错乱的隐患。

### 2026-05-03 诡异Bug修复：解决 `recovery.bat` 出现“谟没柯寄诘?Openclaw”灵异报错的问题
- **问题描述**：用户在运行新建的 `recovery.bat` 时，CMD 控制台突然抛出各种乱码和奇怪的指令报错，比如 `'谟没柯寄诘?Openclaw' 不是内部或外部命令`，脚本完全无法正常执行。
- **原因剖析**：这又是一个 Windows 原生 CMD 极其臭名昭著的底层 Bug。由于先前写盘脚本以 Unix 风格的 `LF (\n)` 而非 Windows 标准的 `CRLF (\r\n)` 换行符保存了 GBK 编码的批处理文件，导致 CMD 解析器在按行读取并在内存中定位指令偏移量（Offset）时发生了致命错位——它刚好算错了 **1 个字节**！
  - 原文：“在用户目录内的 Openclaw”。“在”的 GBK 编码是 `D4 DA`，“用”是 `D3 C3`。
  - 由于偏移错位 1 字节，CMD 直接从 `DA D3 C3 ...` 开始往后念。它把这串错位的字节强行当做命令去执行，而 `DA D3` 刚好凑成了汉字“谟”，`C3 BB` 凑成了“没”，于是就拼成了那一长串灵异的乱码指令，引发了这起让人啼笑皆非的“灵异事件”。
### 2026-05-05 Bug修复：修正 Git 环境变量的路径指向
- **问题描述**：用户发现，即使在安装向导中勾选了“添加 Git 到环境变量”，安装完成后打开命令行输入 `git --version` 依然提示找不到命令（环境变量实际上无效）。
- **原因剖析**：在原本的 `setup.iss` 中，环境变量附加的路径是 `{app}\git`。然而，标准的 Windows 版 Git 便携包，其可执行文件 `git.exe` 实际上存放在 `git\cmd`（或 `git\bin`）深层目录下，而不是直接放在 `git` 根目录。把根目录加进 PATH 自然无法让系统找到执行文件。
- **修复详情**：
  1. 将安装时写入 PATH 的路径由 `ExpandConstant('{app}\git')` 修正为 `ExpandConstant('{app}\git\cmd')`。
  2. 为了兼顾已经使用旧版本安装过的小龙虾实现“原汤化原食”式的无死角卸载，在卸载逻辑 `CurUninstallStepChanged` 中，针对勾选过安装 Git 环境变量的用户，加入了双重抹除指令：同时抹除 `{app}\git` 和 `{app}\git\cmd`。保证了既能纠正新版路径，又能完美擦除老版的残留。
